# Popular packages

Notes:

- The prefix `λ. ` in the example is the GHCI prompt.

## `Data.Maybe`

`import Data.Maybe`

- fromMaybe :: a -> Maybe a -> a

Examples:

```haskell
λ. fromMaybe 10 Nothing
10
```
